# Yvy Python batch-processing dependencies (.plans/terrabrasilis-integration, Inc 1)
# Pinned for OCI A1 (ARM) reproducibility. The DETER/CAR spatial joins and
# PRODES raster handling run as offline Python batch jobs.
geopandas==0.14.4
shapely==2.0.6
requests==2.31.0
rasterio==1.3.10
# Pillow: tile PNG rendering (render_car_tiles.py)
pillow>=10,<12
# reportlab: risk-intelligence PDF reports (render_risk_report.py, Inc 4)
reportlab>=4.0,<5
# matplotlib: P5 static map in the risk report (render_risk_report.py, fixup Inc 3)
matplotlib>=3.7,<4
