name: Node.js CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      - name: Checkout Repository
        uses: actions/checkout@v3

      # Set up Node.js environment
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      # Define o caminho de trabalho se o package.json estiver em uma subpasta
      - name: Change directory to the project folder
        working-directory: ./node  # Ajuste este caminho se necessário

      # Install dependencies
      - name: Install dependencies
        run: npm ci

      # Run the build script (if applicable)
      - name: Build project
        run: npm run build

      # Run tests (if applicable)
      - name: Run tests
        run: npm test

      # Deploy or start the server
      - name: Run server
        run: npm start
