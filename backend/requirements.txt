quart>=0.19,<1.0
quart-cors>=0.7,<1.0
aiosqlite>=0.20,<1.0
httpx>=0.27,<1.0
redis[hiredis]>=5.0,<6.0
hypercorn>=0.17,<1.0
python-dotenv>=1.0,<2.0
# pysqlite3-binary bundles SQLite 3.45+ for JSONB support on older OS packages.
# Not available on all platforms (e.g. Windows Python 3.12) — the monkey-patch
# in db_sqlite.py falls back to stdlib sqlite3 if pysqlite3 is missing.
pysqlite3-binary>=0.5.0; sys_platform == "linux"